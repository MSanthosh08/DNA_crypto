#to download the dataset

mkdir -p data/kodak
cd data/kodak
wget -r -np -nH --cut-dirs=3 -R "index.html*" http://r0k.us/graphics/kodak/
cd ../..


# Encrypt (with default: compression + splicing):

python -m app.main --encrypt data/kodak/kodim01.png --key "my_secret_key" --output encrypted.dna

#Encrypt (skip compression for debugging):

python -m app.main --encrypt data/kodak/kodim01.png --key "my_secret_key" --output encrypted_nocomp.dna --no-compress

#Encrypt (skip splice for debugging):

python -m app.main --encrypt data/kodak/kodim01.png --key "my_secret_key" --output encrypted_nosplice.dna --no-splice

#Decrypt:

python -m app.main --decrypt encrypted.dna --key "my_secret_key" --output decrypted.png



# To verify both are same
python verify.py